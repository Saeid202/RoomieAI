
import PropertyFormContainer from "./PropertyFormContainer";

// This is now just a simple wrapper component that imports the main container
export default function PropertyForm() {
  return <PropertyFormContainer />;
}
